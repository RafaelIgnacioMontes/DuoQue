import { useState } from 'react'

const ProfilePage = ({ summoner }) => {
  return (
    <div className="greaterprofilediv">
      <div>
        {/* {summoner.map((sum) => (
          <header className="summonername">{sum.summonerName}</header>
        ))} */}
      </div>
    </div>
  )
}

export default ProfilePage
